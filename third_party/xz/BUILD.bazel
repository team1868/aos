load("@rules_cc//cc:cc_library.bzl", "cc_library")

cc_library(
    name = "unavailable",
    target_compatible_with = ["@platforms//:incompatible"],
)

alias(
    name = "lzma",
    actual = select({
        "@platforms//cpu:arm64": "@arm64_debian_sysroot//:liblzma",
        "@platforms//cpu:x86_64": "@amd64_debian_sysroot//:liblzma",
        "//conditions:default": ":unavailable",
    }),
    visibility = ["//visibility:public"],
)

# cc_binary(name = "xz")
