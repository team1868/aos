load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "unavailable",
    target_compatible_with = ["@platforms//:incompatible"],
)

alias(
    name = "glib",
    actual = select({
        "@platforms//cpu:arm64": "@arm64_debian_sysroot//:glib-2.0",
        "@platforms//cpu:x86_64": "@amd64_debian_sysroot//:glib-2.0",
        "//conditions:default": ":unavailable",
    }),
)

alias(
    name = "gobject",
    actual = select({
        "@platforms//cpu:arm64": "@arm64_debian_sysroot//:gobject-2.0",
        "@platforms//cpu:x86_64": "@amd64_debian_sysroot//:gobject-2.0",
        "//conditions:default": ":unavailable",
    }),
)

# Alias for gio-2.0 if needed later
alias(
    name = "gio",
    actual = select({
        "@platforms//cpu:arm64": "@arm64_debian_sysroot//:gio-2.0",
        "@platforms//cpu:x86_64": "@amd64_debian_sysroot//:gio-2.0",
        "//conditions:default": ":unavailable",
    }),
)
